(() => {
  'use strict';
  const D=window.DronepadData;
  let db=D.load();
  let lang=D.getLang();
  const page=document.body.dataset.page||'home';
  const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
  const t=path=>{let v=D.i18n[lang];for(const p of path.split('.'))v=v?.[p];return v??path;};
  const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
  const today=()=>new Date().toISOString().slice(0,10), nowIso=()=>new Date().toISOString();
  const save=()=>D.save(db), byId=id=>db.investors.find(x=>String(x.id)===String(id));
  const stageLabel=s=>t(`stages.${s}`), priorityLabel=p=>t(`priority.${p}`);
  const activityLabel=v=>lang==='en'?({Ricerca:'Research',Email:'Email',Call:'Call',Meeting:'Meeting','Follow-up':'Follow-up',Nota:'Note'}[v]||v):v;
  function safeUrl(v){if(!v)return'';try{const u=new URL(v,location.href);return['http:','https:'].includes(u.protocol)?u.href:'';}catch{return'';}}
  function formatDate(v){if(!v)return'—';const d=new Date(`${v}T12:00:00`);return Number.isNaN(d.getTime())?v:d.toLocaleDateString(lang==='it'?'it-IT':'en-GB',{day:'2-digit',month:'short',year:'numeric'});}
  function daysFromToday(v){if(!v)return null;const a=new Date(`${today()}T12:00:00`),b=new Date(`${v}T12:00:00`);return Math.round((b-a)/86400000);}
  function priorityClass(p){return p==='Alta'?'high':p==='Bassa'?'low':'medium';}

  function applyTranslations(){
    document.documentElement.lang=lang;
    $$('[data-i18n]').forEach(el=>el.textContent=t(el.dataset.i18n));
    $$('[data-i18n-html]').forEach(el=>el.innerHTML=t(el.dataset.i18nHtml));
    $$('.lang-switch button').forEach(b=>b.classList.toggle('active',b.dataset.lang===lang));
  }
  function setLanguage(v){lang=v==='en'?'en':'it';D.setLang(lang);applyTranslations();renderPage();}
  function initCommon(){
    applyTranslations();
    $$('.lang-switch button').forEach(b=>b.addEventListener('click',()=>setLanguage(b.dataset.lang)));
    $('#backupBtn')?.addEventListener('click',backup);
    $$('[data-close]').forEach(btn=>btn.addEventListener('click',()=>document.getElementById(btn.dataset.close)?.close()));
    initReveal();
  }
  function backup(){const blob=new Blob([JSON.stringify({exportedAt:nowIso(),product:'Dronepad Investor Hub',data:db},null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`dronepad-investor-hub-${today()}.json`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000);}
  function initReveal(){
    document.body.classList.add('js-ready');
    const els=$$('.reveal');
    if(!('IntersectionObserver'in window)){els.forEach(e=>e.classList.add('visible'));return;}
    const io=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');io.unobserve(e.target);}}),{threshold:.08,rootMargin:'0px 0px -5% 0px'});
    els.forEach(el=>{const r=el.getBoundingClientRect();if(r.top<innerHeight*.96)el.classList.add('visible');else io.observe(el);});
  }

  function metricData(){const total=db.investors.length,advanced=db.investors.filter(x=>x.stage!=='Research').length,contacted=db.investors.filter(x=>['Contacted','Meeting','NDA / DD','Term Sheet'].includes(x.stage)).length,followups=db.investors.filter(x=>x.nextDate&&(daysFromToday(x.nextDate)??-1)>=0).length;return[[t('metric.total'),total],[t('metric.advanced'),advanced],[t('metric.contacted'),contacted],[t('metric.followups'),followups]];}
  function renderMetrics(id){const root=$(id);if(root)root.innerHTML=metricData().map(([l,v])=>`<article class="metric-card"><span>${esc(l)}</span><strong>${v}</strong></article>`).join('');}
  function allActivities(){const rows=[];db.investors.forEach(inv=>(inv.activities||[]).forEach(a=>rows.push({...a,investorId:inv.id,investorName:inv.name})));return rows.sort((a,b)=>String(b.date||b.createdAt||'').localeCompare(String(a.date||a.createdAt||'')));}

  function renderHome(){
    renderMetrics('#dashboardMetrics');
    const funnel=$('#dashboardFunnel');if(funnel)funnel.innerHTML=D.STAGES.map(s=>`<div class="funnel-item"><span>${esc(stageLabel(s))}</span><b>${db.investors.filter(x=>x.stage===s).length}</b></div>`).join('');
    const follow=$('#dashboardFollowups');if(follow){const list=db.investors.filter(x=>x.nextDate&&(daysFromToday(x.nextDate)??-1)>=0).sort((a,b)=>a.nextDate.localeCompare(b.nextDate)).slice(0,6);follow.innerHTML=list.length?list.map(x=>`<a class="list-row" href="investor.html?id=${encodeURIComponent(x.id)}"><time>${esc(formatDate(x.nextDate))}</time><div><b>${esc(x.name)}</b><small>${esc(x.nextAction||'—')}</small></div></a>`).join(''):`<div class="empty">${esc(t('empty.followups'))}</div>`;}
    const acts=$('#dashboardActivities');if(acts){const list=allActivities().slice(0,7);acts.innerHTML=list.length?list.map(a=>`<a class="list-row" href="investor.html?id=${encodeURIComponent(a.investorId)}"><time>${esc(formatDate(a.date))}</time><div><b>${esc(a.title)}</b><small>${esc(a.investorName)} · ${esc(activityLabel(a.type))}${a.details?` · ${esc(a.details)}`:''}</small></div></a>`).join(''):`<div class="empty">${esc(t('empty.activities'))}</div>`;}
  }

  function fillOptions(form){
    form.elements.type.innerHTML=D.TYPES.map(v=>`<option>${esc(v)}</option>`).join('');
    form.elements.stage.innerHTML=D.STAGES.map(v=>`<option value="${esc(v)}">${esc(stageLabel(v))}</option>`).join('');
    form.elements.priority.innerHTML=D.PRIORITIES.map(v=>`<option value="${esc(v)}">${esc(priorityLabel(v))}</option>`).join('');
  }
  function openInvestorModal(inv=null){const d=$('#investorDialog'),f=$('#investorForm');if(!d||!f)return;fillOptions(f);f.reset();const vals={id:inv?.id||'',name:inv?.name||'',type:inv?.type||D.TYPES[0],stage:inv?.stage||'Research',priority:inv?.priority||'Media',contactName:inv?.contactName||'',contactRole:inv?.contactRole||'',contactEmail:inv?.contactEmail||'',contactPhone:inv?.contactPhone||'',website:inv?.website||'',lastContact:inv?.lastContact||'',nextDate:inv?.nextDate||'',thesis:inv?.thesis||'',source:inv?.source||'',nextAction:inv?.nextAction||'',notes:inv?.notes||''};Object.entries(vals).forEach(([k,v])=>{if(f.elements[k])f.elements[k].value=v;});$('#investorDialogTitle').textContent=inv?(lang==='it'?'Modifica prospect':'Edit prospect'):(lang==='it'?'Aggiungi prospect':'Add prospect');d.showModal();}
  function saveInvestorForm(form){const fd=new FormData(form),id=String(fd.get('id')||''),existing=id?byId(id):null,oldStage=existing?.stage,item=existing||{id:D.uid('inv'),createdAt:nowIso(),activities:[]};['name','type','stage','priority','contactName','contactRole','contactEmail','contactPhone','website','lastContact','nextDate','thesis','source','nextAction','notes'].forEach(k=>item[k]=String(fd.get(k)||'').trim());item.updatedAt=nowIso();if(!existing){item.activities.push({id:D.uid('act'),date:today(),type:'Nota',title:lang==='it'?'Prospect inserito nella pipeline':'Prospect added to pipeline',details:item.source?`${lang==='it'?'Fonte':'Source'}: ${item.source}`:'',nextAction:item.nextAction,nextDate:item.nextDate,createdAt:nowIso()});db.investors.unshift(item);}else if(oldStage!==item.stage){item.activities.unshift({id:D.uid('act'),date:today(),type:'Nota',title:lang==='it'?'Stadio pipeline aggiornato':'Pipeline stage updated',details:`${stageLabel(oldStage)} → ${stageLabel(item.stage)}`,nextAction:item.nextAction,nextDate:item.nextDate,createdAt:nowIso()});}save();return item;}
  function removeInvestor(inv,redirect=false){if(!inv)return;if(!confirm(lang==='it'?`Eliminare "${inv.name}" dalla pipeline?`:`Delete "${inv.name}" from the pipeline?`))return;db.investors=db.investors.filter(x=>x.id!==inv.id);save();redirect?location.href='pipeline.html':renderPipeline();}

  function fillFilters(){const s=$('#stageFilter'),ty=$('#typeFilter'),p=$('#priorityFilter');const sv=s?.value||'all',tv=ty?.value||'all',pv=p?.value||'all';if(s){s.innerHTML=`<option value="all">${lang==='it'?'Tutti gli stadi':'All stages'}</option>`+D.STAGES.map(v=>`<option value="${esc(v)}">${esc(stageLabel(v))}</option>`).join('');s.value=D.STAGES.includes(sv)?sv:'all';}if(ty){ty.innerHTML=`<option value="all">${lang==='it'?'Tutti i tipi':'All types'}</option>`+D.TYPES.map(v=>`<option value="${esc(v)}">${esc(v)}</option>`).join('');ty.value=D.TYPES.includes(tv)?tv:'all';}if(p){p.innerHTML=`<option value="all">${lang==='it'?'Tutte le priorità':'All priorities'}</option>`+D.PRIORITIES.map(v=>`<option value="${esc(v)}">${esc(priorityLabel(v))}</option>`).join('');p.value=D.PRIORITIES.includes(pv)?pv:'all';}if($('#searchInput'))$('#searchInput').placeholder=lang==='it'?'Cerca nome, tipo, referente o note...':'Search name, type, contact or notes...';}
  function filtered(){const q=($('#searchInput')?.value||'').toLowerCase(),s=$('#stageFilter')?.value||'all',ty=$('#typeFilter')?.value||'all',p=$('#priorityFilter')?.value||'all';return db.investors.filter(x=>[x.name,x.type,x.contactName,x.contactRole,x.contactEmail,x.thesis,x.source,x.nextAction,x.notes].join(' ').toLowerCase().includes(q)&&(s==='all'||x.stage===s)&&(ty==='all'||x.type===ty)&&(p==='all'||x.priority===p));}
  function renderPipeline(){renderMetrics('#pipelineMetrics');fillFilters();const list=filtered();if($('#pipelineCount'))$('#pipelineCount').textContent=list.length;const rows=$('#pipelineRows');if(rows)rows.innerHTML=list.length?list.map(x=>`<tr><td><a class="investor-name" href="investor.html?id=${encodeURIComponent(x.id)}">${esc(x.name)}</a><small>${esc(x.thesis||x.source||'—')}</small></td><td>${esc(x.type)}</td><td><select class="stage-select" data-stage-id="${esc(x.id)}">${D.STAGES.map(s=>`<option value="${esc(s)}" ${s===x.stage?'selected':''}>${esc(stageLabel(s))}</option>`).join('')}</select></td><td><span class="tag ${priorityClass(x.priority)}">${esc(priorityLabel(x.priority))}</span></td><td>${esc(formatDate(x.lastContact))}</td><td><b>${esc(x.nextAction||'—')}</b><small>${esc(formatDate(x.nextDate))}</small></td><td><div class="action-row"><a class="table-action" href="investor.html?id=${encodeURIComponent(x.id)}">${esc(t('actions.open'))}</a><button class="table-action" data-edit-id="${esc(x.id)}">${esc(t('actions.edit'))}</button><button class="table-action danger" data-delete-id="${esc(x.id)}">${esc(t('actions.delete'))}</button></div></td></tr>`).join(''):`<tr><td colspan="7"><div class="empty">${esc(t('empty.pipeline'))}</div></td></tr>`;const cards=$('#pipelineCards');if(cards)cards.innerHTML=list.map(x=>`<article class="mobile-card"><div class="mobile-card-head"><div><a class="investor-name" href="investor.html?id=${encodeURIComponent(x.id)}">${esc(x.name)}</a><small>${esc(x.type)}</small></div><span class="tag ${priorityClass(x.priority)}">${esc(priorityLabel(x.priority))}</span></div><p>${esc(x.thesis||'—')}</p><div class="mobile-meta"><div><span>${esc(t('table.stage'))}</span><b>${esc(stageLabel(x.stage))}</b></div><div><span>${esc(t('table.next'))}</span><b>${esc(x.nextAction||'—')}</b></div></div><div class="action-row"><a class="table-action" href="investor.html?id=${encodeURIComponent(x.id)}">${esc(t('actions.open'))}</a><button class="table-action" data-edit-id="${esc(x.id)}">${esc(t('actions.edit'))}</button><button class="table-action danger" data-delete-id="${esc(x.id)}">${esc(t('actions.delete'))}</button></div></article>`).join('');$$('[data-edit-id]').forEach(b=>b.onclick=()=>openInvestorModal(byId(b.dataset.editId)));$$('[data-delete-id]').forEach(b=>b.onclick=()=>removeInvestor(byId(b.dataset.deleteId)));$$('[data-stage-id]').forEach(sel=>sel.onchange=()=>{const inv=byId(sel.dataset.stageId),old=inv.stage;inv.stage=sel.value;if(['Contacted','Meeting'].includes(inv.stage)&&!inv.lastContact)inv.lastContact=today();if(old!==inv.stage)inv.activities.unshift({id:D.uid('act'),date:today(),type:'Nota',title:lang==='it'?'Stadio pipeline aggiornato':'Pipeline stage updated',details:`${stageLabel(old)} → ${stageLabel(inv.stage)}`,nextAction:inv.nextAction,nextDate:inv.nextDate,createdAt:nowIso()});save();renderPipeline();});}
  function initPipeline(){fillFilters();$('#addInvestorBtn')?.addEventListener('click',()=>openInvestorModal());['#searchInput','#stageFilter','#typeFilter','#priorityFilter'].forEach(s=>{const el=$(s);if(el)el.addEventListener(el.tagName==='INPUT'?'input':'change',renderPipeline);});$('#investorForm')?.addEventListener('submit',e=>{e.preventDefault();saveInvestorForm(e.currentTarget);$('#investorDialog').close();renderPipeline();});renderPipeline();}

  function currentInvestor(){return byId(new URLSearchParams(location.search).get('id'));}
  function fillDetailForm(inv){const f=$('#investorForm');fillOptions(f);['id','name','type','stage','priority','contactName','contactRole','contactEmail','contactPhone','website','lastContact','nextDate','thesis','source','nextAction','notes'].forEach(k=>f.elements[k].value=inv[k]||'');}
  function fillActivityOptions(f){f.elements.type.innerHTML=D.ACTIVITY_TYPES.map(v=>`<option value="${esc(v)}">${esc(activityLabel(v))}</option>`).join('');}
  function openActivity(inv,a=null){const f=$('#activityForm');fillActivityOptions(f);f.reset();f.elements.id.value=a?.id||'';f.elements.date.value=a?.date||today();f.elements.type.value=a?.type||'Ricerca';f.elements.title.value=a?.title||'';f.elements.details.value=a?.details||'';f.elements.nextAction.value=a?.nextAction||'';f.elements.nextDate.value=a?.nextDate||'';$('#activityDialogTitle').textContent=a?(lang==='it'?'Modifica attività':'Edit activity'):(lang==='it'?'Registra attività':'Add activity');$('#activityDialog').showModal();}
  function saveActivity(inv,form){const fd=new FormData(form),id=String(fd.get('id')||'');inv.activities=inv.activities||[];let a=id?inv.activities.find(x=>x.id===id):null;if(!a){a={id:D.uid('act'),createdAt:nowIso()};inv.activities.unshift(a);}Object.assign(a,{date:String(fd.get('date')||today()),type:String(fd.get('type')||'Ricerca'),title:String(fd.get('title')||'').trim(),details:String(fd.get('details')||'').trim(),nextAction:String(fd.get('nextAction')||'').trim(),nextDate:String(fd.get('nextDate')||'')});if(['Email','Call','Meeting','Follow-up'].includes(a.type))inv.lastContact=a.date;if(a.nextAction)inv.nextAction=a.nextAction;if(a.nextDate)inv.nextDate=a.nextDate;inv.updatedAt=nowIso();save();}
  function renderDetail(){const inv=currentInvestor();if(!inv){$('.app-main').innerHTML=`<div class="empty">${esc(t('empty.notFound'))}</div>`;return;}document.title=`${inv.name} — Dronepad Investor Hub`;$('#detailType').textContent=inv.type;$('#detailName').textContent=inv.name;$('#detailBadges').innerHTML=`<span class="tag medium">${esc(stageLabel(inv.stage))}</span><span class="tag ${priorityClass(inv.priority)}">${esc(priorityLabel(inv.priority))}</span>`;const site=safeUrl(inv.website),info=[[t('form.contactName'),inv.contactName?`${inv.contactName}${inv.contactRole?` · ${inv.contactRole}`:''}`:'—'],['Email',inv.contactEmail||'—'],[t('form.phone'),inv.contactPhone||'—'],[t('form.website'),site?`<a href="${esc(site)}" target="_blank" rel="noopener">${esc(inv.website)}</a>`:'—'],[t('form.lastContact'),formatDate(inv.lastContact)],[t('form.nextDate'),formatDate(inv.nextDate)]];$('#detailInfo').innerHTML=info.map(([k,v])=>`<div class="info-item"><span>${esc(k)}</span><b>${v}</b></div>`).join('');$('#detailNext').innerHTML=`<strong>${esc(inv.nextAction||'—')}</strong><time>${esc(formatDate(inv.nextDate))}</time>`;$('#detailThesis').textContent=inv.thesis||'—';$('#detailSource').textContent=inv.source?`${t('form.source')}: ${inv.source}`:'';$('#detailNotes').textContent=inv.notes||'—';const acts=[...(inv.activities||[])].sort((a,b)=>String(b.date).localeCompare(String(a.date)));$('#detailTimeline').innerHTML=acts.length?acts.map(a=>`<article class="timeline-item"><div class="timeline-date"><b>${esc(formatDate(a.date))}</b><span>${esc(activityLabel(a.type))}</span></div><div class="timeline-copy"><h3>${esc(a.title)}</h3><p>${esc(a.details||'')}</p>${a.nextAction?`<div class="timeline-next"><b>${esc(t('form.nextAction'))}:</b> ${esc(a.nextAction)}${a.nextDate?` · ${esc(formatDate(a.nextDate))}`:''}</div>`:''}</div><div class="action-row"><button class="table-action" data-edit-activity="${esc(a.id)}">${esc(t('actions.edit'))}</button><button class="table-action danger" data-delete-activity="${esc(a.id)}">${esc(t('actions.delete'))}</button></div></article>`).join(''):`<div class="empty">${esc(t('empty.activities'))}</div>`;$$('[data-edit-activity]').forEach(b=>b.onclick=()=>openActivity(inv,inv.activities.find(a=>a.id===b.dataset.editActivity)));$$('[data-delete-activity]').forEach(b=>b.onclick=()=>{if(confirm(lang==='it'?'Eliminare questa attività?':'Delete this activity?')){inv.activities=inv.activities.filter(a=>a.id!==b.dataset.deleteActivity);save();renderDetail();}});$('#editInvestorBtn').onclick=()=>{fillDetailForm(inv);$('#investorDialog').showModal();};$('#deleteInvestorBtn').onclick=()=>removeInvestor(inv,true);$('#addActivityBtn').onclick=()=>openActivity(inv);const f=$('#investorForm');if(!f.dataset.bound){f.dataset.bound=1;f.addEventListener('submit',e=>{e.preventDefault();saveInvestorForm(f);$('#investorDialog').close();renderDetail();});}const af=$('#activityForm');if(!af.dataset.bound){af.dataset.bound=1;af.addEventListener('submit',e=>{e.preventDefault();saveActivity(currentInvestor(),af);$('#activityDialog').close();renderDetail();});}}

  function renderPage(){if(page==='home')renderHome();else if(page==='pipeline')renderPipeline();else renderDetail();}
  initCommon(); if(page==='pipeline')initPipeline(); else renderPage();
})();
